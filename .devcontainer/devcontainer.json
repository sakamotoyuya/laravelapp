{
    "name": "Laravel Dev",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "forwardPorts": [
        8000
    ],
    "postCreateCommand": "composer install && cp .env.example .env && php artisan key:generate && php artisan serve --host=0.0.0.0"
}